# 💊 ระบบแจ้งเตือนการกินยา (Medication Reminder System)

ระบบเว็บไซต์สำหรับจัดการและแจ้งเตือนการกินยาอย่างง่ายดาย พัฒนาด้วย HTML, CSS, JavaScript และ PHP พร้อม SQLite Database

## 🎯 ฟีเจอร์หลัก

- ✅ **ง่ายต่อการใช้งาน**: ออกแบบ UI/UX ให้เข้าใจง่าย ไม่ซับซ้อน
- ✅ **ระบบ Step-by-Step**: แบ่งขั้นตอนการใช้งานอย่างชัดเจน
- ✅ **รองรับยาหลากหลายชนิด**: มีรายการยาทั่วไปให้เลือก หรือกรอกเอง
- ✅ **การแจ้งเตือนแบบ Real-time**: แจ้งเตือนผ่านเบราว์เซอร์และป๊อปอัพ
- ✅ **ทำงานแบบออฟไลน์**: ใช้งานได้แม้ไม่มีอินเทอร์เน็ต
- ✅ **Responsive Design**: ใช้งานได้ทั้งบนมือถือและคอมพิวเตอร์
- ✅ **ฐานข้อมูล SQLite**: ง่ายต่อการติดตั้งและจัดการ

## 📁 โครงสร้างไฟล์

```
medication-reminder/
├── index.html              # หน้าเว็บหลัก
├── style.css               # ไฟล์ตกแต่ง
├── script.js               # JavaScript หลัก
├── database.php            # การตั้งค่าฐานข้อมูล
├── save_reminder.php       # API บันทึกข้อมูล
├── get_reminders.php       # API ดึงข้อมูล
├── medication_reminders.db # ฐานข้อมูล SQLite (จะสร้างอัตโนมัติ)
└── README.md               # คู่มือนี้
```

## 🚀 วิธีการติดตั้ง

### วิธีที่ 1: ใช้ XAMPP (แนะนำสำหรับมือใหม่)

1. **ดาวน์โหลดและติดตั้ง XAMPP**
   - ไปที่ [https://www.apachefriends.org](https://www.apachefriends.org)
   - ดาวน์โหลดและติดตั้ง XAMPP

2. **วางไฟล์โปรเจกต์**
   - คัดลอกไฟล์ทั้งหมดไปใส่ในโฟลเดอร์ `C:\xampp\htdocs\medication-reminder\`

3. **เปิด XAMPP และเริ่มเซิร์ฟเวอร์**
   - เปิดโปรแกรม XAMPP Control Panel
   - กด "Start" ที่ Apache และ MySQL (ถ้าต้องการ)

4. **เข้าใช้งานระบบ**
   - เปิดเบราว์เซอร์และไปที่ `http://localhost/medication-reminder/`

### วิธีที่ 2: ใช้ PHP Built-in Server

1. **ตรวจสอบ PHP**
   ```bash
   php --version
   ```

2. **เข้าไปในโฟลเดอร์โปรเจกต์**
   ```bash
   cd medication-reminder
   ```

3. **รันเซิร์ฟเวอร์**
   ```bash
   php -S localhost:8000
   ```

4. **เข้าใช้งาน**
   - เปิดเบราว์เซอร์และไปที่ `http://localhost:8000`

### วิธีที่ 3: Deploy บน Shared Hosting

1. **อัพโหลดไฟล์**
   - อัพโหลดไฟล์ทั้งหมดไปยัง public_html หรือ www ของ hosting

2. **ตั้งค่าสิทธิ์**
   - ตั้งค่าสิทธิ์โฟลเดอร์ให้ PHP สามารถเขียนไฟล์ฐานข้อมูลได้ (chmod 755)

3. **เข้าใช้งาน**
   - เข้าใช้งานผ่าน domain ของคุณ

## 📱 วิธีการใช้งาน

### ขั้นตอนที่ 1: กรอกชื่อเล่น
- กรอกชื่อเล่นของคุณเพื่อระบบจะได้แจ้งเตือนด้วยชื่อที่ถูกต้อง

### ขั้นตอนที่ 2: เลือกชื่อยา
- เลือกจากรายการยาทั่วไป หรือ
- เลือก "อื่นๆ (กรอกเอง)" แล้วกรอกชื่อยาที่ต้องการ

### ขั้นตอนที่ 3: กำหนดเวลาแจ้งเตือน
- กำหนดระยะเวลาแจ้งเตือน เช่น "ทุก 6 ชั่วโมง" หรือ "ทุก 8 ชั่วโมง 30 นาที"
- เลือกเวลาแรกที่ต้องกินยา

### ขั้นตอนที่ 4: เพิ่มหมายเหตุ (ไม่บังคับ)
- เลือกหมายเหตุ เช่น "หลังอาหารเช้า", "ก่อนนอน" หรือ
- กรอกหมายเหตุเอง

### ขั้นตอนที่ 5: จัดการรายการยาและเริ่มใช้งาน
- ตรวจสอบข้อมูลและกด "เพิ่มยานี้"
- สามารถเพิ่มยาหลายรายการได้
- กด "เริ่มใช้งานระบบแจ้งเตือน" เมื่อเสร็จแล้ว

## ⚙️ การตั้งค่าเพิ่มเติม

### เปิดใช้งาน Browser Notifications

1. เมื่อระบบขอสิทธิ์แสดง notification ให้กด "Allow"
2. หากพลาด สามารถเปิดใช้ได้ผ่าน:
   - Chrome: Settings > Privacy and security > Site Settings > Notifications
   - Firefox: Preferences > Privacy & Security > Notifications

### ตั้งค่า Timezone

- ระบบใช้ timezone Asia/Bangkok เป็นค่าเริ่มต้น
- หากต้องการเปลี่ยน แก้ไขในไฟล์ `database.php` ที่บรรทัด:
  ```php
  date_default_timezone_set('Asia/Bangkok');
  ```

## 🔧 การแก้ไขปัญหาที่พบบ่อย

### ปัญหา: ไม่สามารถสร้างฐานข้อมูลได้

**สาเหตุ**: PHP ไม่มีสิทธิ์เขียนไฟล์
**วิธีแก้**:
```bash
chmod 755 medication-reminder/
chmod 666 medication-reminder/medication_reminders.db
```

### ปัญหา: การแจ้งเตือนไม่ทำงาน

**สาเหตุ**: Browser ปิดการแจ้งเตือน
**วิธีแก้**:
1. ตรวจสอบ notification permission ในเบราว์เซอร์
2. ลองรีเฟรชหน้าเว็บ
3. ตรวจสอบว่าเปิดแท็บไว้หรือไม่

### ปัญหา: ข้อมูลหายเมื่อปิดเบราว์เซอร์

**สาเหตุ**: ทำงานแบบออฟไลน์และไม่ได้เซฟไปฐานข้อมูล
**วิธีแก้**:
1. ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
2. ตรวจสอบว่าเซิร์ฟเวอร์ PHP ทำงานหรือไม่
3. ข้อมูลจะถูกเก็บใน localStorage ของเบราว์เซอร์

## 🎨 การปรับแต่ง

### เปลี่ยนสีธีม

แก้ไขไฟล์ `style.css`:
```css
:root {
  --primary-color: #007bff;    /* สีหลัก */
  --secondary-color: #6c757d;  /* สีรอง */
  --success-color: #28a745;    /* สีสำเร็จ */
  --warning-color: #ffc107;    /* สีเตือน */
  --danger-color: #dc3545;     /* สีอันตราย */
}
```

### เพิ่มยาในรายการ

แก้ไขไฟล์ `index.html` ส่วน `medicationSelect`:
```html
<option value="ยาใหม่">ยาใหม่ (ชื่อยา)</option>
```

### ปรับระยะเวลาแจ้งเตือนเริ่มต้น

แก้ไขไฟล์ `index.html`:
```html
<input type="number" id="reminderHours" value="6">  <!-- เปลี่ยนเป็นชั่วโมงที่ต้องการ -->
```

## 📊 ฐานข้อมูล

### โครงสร้างตาราง

**ตาราง users**
- id (Primary Key)
- name (ชื่อผู้ใช้)
- created_at, updated_at

**ตาราง medications**
- id (Primary Key)
- user_id (Foreign Key)
- name (ชื่อยา)
- hours, minutes (ระยะเวลาแจ้งเตือน)
- first_time (เวลาแรก)
- note (หมายเหตุ)
- is_active (สถานะ)
- created_at, updated_at

**ตาราง reminder_logs**
- id (Primary Key)
- medication_id (Foreign Key)
- reminder_time (เวลาแจ้งเตือน)
- status (สถานะ)
- created_at

## 🔐 ความปลอดภัย

- ข้อมูลทั้งหมดผ่านการ sanitize ก่อนบันทึก
- ใช้ PDO prepared statements ป้องกัน SQL Injection
- ข้อมูลส่วนตัวเก็บไว้ในเครื่องเท่านั้น

## 🚀 การพัฒนาต่อ

### เพิ่มฟีเจอร์ใหม่

1. **ส่ง Email/SMS**
   - ใช้ PHPMailer หรือ Twilio API
   - แก้ไขไฟล์ `script.js` ในฟังก์ชัน `showMedicationReminder()`

2. **รายงานสถิติ**
   - สร้างหน้า dashboard ใหม่
   - ใช้ Chart.js สำหรับกราฟ

3. **ระบบผู้ใช้หลายคน**
   - เพิ่ม login system
   - แก้ไขฐานข้อมูลให้รองรับ sessions

4. **แอพมือถือ**
   - ใช้ Progressive Web App (PWA)
   - เพิ่ม Service Worker

## 🤝 การสนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:

1. ตรวจสอบ Console ของเบราว์เซอร์ (F12)
2. ตรวจสอบ PHP Error Log
3. ทดสอบการเชื่อมต่อฐานข้อมูล

## 📄 License

โปรเจกต์นี้เป็น Open Source สามารถใช้งาน แก้ไข และแจกจ่ายได้อย่างอิสระ

## 🎉 ขอบคุณ

- Bootstrap สำหรับ CSS Framework
- Font Awesome สำหรับ Icons
- Google Fonts สำหรับฟอนต์ Sarabun

---

**หมายเหตุ**: ระบบนี้เป็นเครื่องมือช่วยเตือนเท่านั้น ไม่ใช่คำแนะนำทางการแพทย์ กรุณาปรึกษาแพทย์หรือเภสัชกรสำหรับการใช้ยาที่ถูกต้อง